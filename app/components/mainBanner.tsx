import React from "react";

type Props = {};

export default function MainBanner({}: Props) {
  return (
    <div className="h-[400px]">
      <div className="w-full h-full bg-gray-500">{/* Create slider */}</div>
    </div>
  );
}
